import { AppConfig } from "remax/wechat";

const config: AppConfig = {
  pages: ["pages/index/index"],
  window: {
    navigationBarTitleText: "title",
    navigationBarBackgroundColor: "#4e77ef",
  },
};

export default config;
